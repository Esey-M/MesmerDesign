# Hugo build output & caches
/public/
/resources/_gen/
.hugo_build.lock

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node (only if you later add a CMS / tooling)
node_modules/

# Full-res source images (originals kept locally, not in repo)
intake/source/

# Pinterest bulk-pins CSV (local marketing artifact, regenerated per batch)
pinterest-bulk-pins.csv
